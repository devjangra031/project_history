<!DOCTYPE html>
<html>
  <head>
    <title>ProjectHistory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <nav class="bg-gray-800 p-2 text-black flex justify-between">
        <div>
          <%= link_to "Home", root_path, class: "text-white font-bold" %>
        </div>
        <div>
          <% if user_signed_in? %>
            <span>Welcome, <%= current_user.email %></span>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "ml-4 text-red-400 hover:text-red-300" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "text-green-400 hover:text-green-300" %>
          <% end %>
        </div>
      </nav>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>

    </main>
  </body>
</html>
